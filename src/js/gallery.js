/* ============================================================
   GALLERY.JS — Instagram Feed (Behold.so Widget)
   Amour Affairs · Premium Wedding Photography
   ============================================================ */

// The Behold widget is a native web component loaded from w.behold.so/widget.js
// No custom rendering needed — the widget handles everything.

export function initInstagramFeed() {
  // No-op: widget self-initialises via the <behold-widget> custom element
}

export function initGalleryHovers() {
  // No-op: hover effects handled natively by the Behold widget
}
